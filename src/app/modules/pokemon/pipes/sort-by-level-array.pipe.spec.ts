import { SortByLevelArrayPipe } from './sort-by-level-array.pipe';

describe('SortByLevelArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByLevelArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
