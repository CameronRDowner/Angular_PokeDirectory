import { GamesToGameHelper } from './games-to-game-helper';

describe('GamesToGameHelper', () => {
  it('should create an instance', () => {
    expect(new GamesToGameHelper()).toBeTruthy();
  });
});
