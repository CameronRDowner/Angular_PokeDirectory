import { SelectedGameHelper } from './selected-game-helper';

describe('SelectedGameHelper', () => {
  it('should create an instance', () => {
    expect(new SelectedGameHelper()).toBeTruthy();
  });
});
